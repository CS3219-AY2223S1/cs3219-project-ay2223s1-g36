version: "3.9"
services:
  matching-service:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - sqlitevolume:/app/db/db
    environment:
      - NODE_ENV=development
    networks:
      - my-network

volumes:
  sqlitevolume:

networks:
  my-network:
    name: my-network